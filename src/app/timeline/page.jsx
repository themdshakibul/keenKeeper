import Timeline from "@/Components/TimelinePage/Timeline";

export const metadata = {
  title: "KeenKeeper | Timeline page",
  description: "This is Timeline page text call and video call",
};

const TimelinePage = () => {
  return (
    <section>
      <Timeline />
    </section>
  );
};

export default TimelinePage;
